# Session 2026-08-24

## Gjort

### v0.14.0-beta — beta-feedback-fikser + fem nye features (→ alpha)

Hele økten på branch `claude/autobuild-deploy-github-merge-664bda`, merget til main som release v0.14.0-beta (versionCode 15).

#### Fire feilrettinger fra beta-testing (`d4edb75`)
- **Toast bak resttimeren**: RestBar er rot-montert og tegnes alltid over skjerminnhold; `UndoToast` fikk `bottomOffset`-prop og løftes `REST_BAR_CLEARANCE` mens hvile kjører.
- **Hvilevarsler (ekte rotårsak)**: expo-notifications bruker eksakt alarm kun når `canScheduleExactAlarms()` — appen deklarerte aldri `USE_EXACT_ALARM`, så Android 12+ fikk inexakte alarmer som Doze utsetter i minuttvis. Deklarert i app.json (auto-innvilget API 33+). v0.13-fiksen (kanal+permission) var nødvendig, men ikke tilstrekkelig.
- **Systemnav dekket innhold**: edge-to-edge uten bunn-inset noe sted; `Screen`-wrapperen inkluderer nå `bottom`-kanten (alle 10 skjermer, ett sted).
- **Loggen glemte øvelsen ved fanebytte**: drawer-nav remounter skjermen; pager-posisjonen huskes nå i modul-minne + persisteres (`logPagerAnchor`) under aktiv økt, nøklet på anchor-key.

#### Ny funksjonalitet
- **Oppvarmingsramp** (`3b731ef`): «Oppvarming»-ekspander på øvelseskortet (stang×10 → 40/60/80 %), ett-trykks logging som warmup-sett. Første UI-vei som skriver `is_warmup`. Ren logikk i `src/warmupRamp.ts` (10 tester); steg under stangvekt umulige.
- **Comeback-ramp** (`d6bea67`): ny advice-kind `comebackRamp` i progresjonsmotoren — halve avstanden tilbake til pre-pause-toppen per økt, viker ved RPE ≥ 9 / bomma reps, dør ut ved gammel topp. Historikkvindu 4→5 økter. 8 tester.
- **Live øktstatistikk** (`81ff587`): ØKT-kortet viser sett fullført/planlagt (+bonus), volum og PR-teller live. Telle-logikk trukket ut som `summarizeSessionSets()` (deles med sluttoppsummeringen, 6 tester).
- **Muskelbalanse** (`a39f381`): status per muskelgruppe (beregnet fra før, aldri rendret!) vises nå som farget badge + push/pull-forhold med vurdering ved > 1,5:1 skjevhet.
- **Førsteøkt-tips** (`c54ccf2`): to nye HintBanner-engangstips (ALT-bytte, supersett-lenking), forskjøvet på sett-antall. Backlog-punkt lukket.
- **Live hvile-nedtelling** (`c0c1b27`): ny lokal Expo-modul `modules/rest-countdown` (Kotlin) — stille ongoing-varsel med system-kronometer i nedtellingsmodus, `setTimeoutAfter` fjerner det når hvilen er ute. `requireOptionalNativeModule` gjør JS-siden no-op i eldre binærer/iOS/Expo Go. **Første EAS-build med modulen må røyk-testes på enhet.**
- **PR-minne** (`4de713b`): sessionPrExIds flyttet til modulnivå + persistert — fikset også gammel svakhet der sluttoppsummeringen mistet PR-er etter fanebytte.

#### UI-polish
- Alle 7 horisontale chip-rader (hopp-til, filtre, utstyr/tags) blør nå til container-kanten i stedet for å klippes ved indre padding (`5fa9f73`, `67aa756`).

### CI/CD: autobuild ved merge til main (tidligere samme dag)
- `.github/workflows/release-android.yml`: guard-jobb (`scripts/ci-should-release.js`) sammenligner versionCode HEAD mot HEAD^; bygger kun ved bump. Manglende `EXPO_TOKEN` → skip med warning (ikke rød). Verifisert live: SKIP ved uendret versionCode, RELEASE-deteksjon + token-warning ved bump.
- Tag-triggeren i `.eas/workflows/release-android.yml` fjernet (duplikat-versionCode-risiko); kjøres manuelt via `eas workflow:run`.
- `set-release-notes.js` godtar nøkkelen via `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON`-env (for CI).
- GitHub Pages-bygget (rødt siden juni) fikset: `docs/_config.yml` ekskluderer interne docs fra Jekyll — personvernerklæringen er igjen deploybar.
- v0.13.0-beta (vc14) ble bygget + submittet + release notes satt (den var ferdig på main, men aldri bygget). NB: auto-submit med `--no-wait` landet aldri i Play; eksplisitt `eas submit --id` funket — dokumentert i RELEASE.md.

## Verifisering
- tsc 0 feil, 261 tester grønne (24 nye), lint 0 errors (158 warnings, netto −1 fra økten start).
- NB: `npm run verify` finner 0 tester inne i Claude-worktrees (`/.claude/` i testPathIgnorePatterns matcher worktree-stien) — kjørt med overstyring; egen oppfølgingsoppgave spawnet for fiks.

## Neste
- Røyk-test på enhet etter at vc15 er ute: eksakte hvilevarsler, live nedtelling i varselfeltet, oppvarmingsramp, comeback-ramp-hint.
- Marius må legge inn `EXPO_TOKEN` (+ valgfritt `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON`) som repo-secrets for at merge-to-main-release skal kjøre selv.
- Gjenstående backlog-punkt: fatigue-trend fra RPE-data.

## Etterspill samme kveld: v0.14.1-beta (vc16) — ute paa alpha

- **Play avviste vc15**: USE_EXACT_ALARM-erklaeringen tillater kun vekkerklokke/kalender som kjernefunksjon. AA feilerklaere var uaktuelt; utgave-utkastet i Play ble forkastet (via Chrome / Play Console — erklaeringsskjemaet dukker foerst opp etter en manuell UI-opplasting av en AAB med tillatelsen, saa Marius dro AAB-en inn selv).
- **Loesning**: SCHEDULE_EXACT_ALARM (ingen Play-erklaering, brukerinnvilget paa Android 14+) + canScheduleExactAlarms()/openExactAlarmSettings() i rest-countdown-modulen + ExactAlarmBanner paa loggskjermen (aapner systemets «Alarmer og paaminnelser», re-sjekker ved resume).
- **To Kotlin-byggfeil fikset underveis**: manglende defaultConfig.versionName i modulens build.gradle, og bare return@Function (typeinferens-brudd i expo-modules-lambdaer).
- **Varsel-hygiene** (beta-skjermbilder: 2x «Hviledagssjekk» daglig med toggle AV + «Hvil ferdig!» 6 t forsinket): kansellering etter data.type i stedet for lagret id, oppstarts-synk mot toggles (rydder foreldreloese hos testerne automatisk), rest-varsler ryddes naar ingen timer resumes, utdatert Expo Go-notis fjernet.
- v0.14.0-notatene foldet inn i 0.14.1; «Hva er nytt» satt paa alpha (en-US). Bekreftet i Play API: track=alpha, versionCodes=[16], completed.
- **Roeyk-test paa enhet gjenstaar**: eksakt-alarm-banner + tilgangsflyt, live nedtelling i varselfeltet, at spoekelsesvarslene forsvinner ved foerste oppstart.
