# Session 2026-08-25

## Gjort

### Supersett-splitting av program-supersett (`a13629b`)
Split-knappen (link-off) i supersett-headeren virker nå også på program-definerte supersett — per-økt (`splitSupersets`-setting), programmet røres ikke, re-lenking via eksisterende manuell merge. Ren `splitProgramSupersets()` i superset.ts, 6 tester.

### Testerfeedback-gjennomgang (22 åpne punkter → alle behandlet)

**Fikset i denne økten:**
- «Legg til øvelse virker ikke» → øvelsen ble lagt til bakerst uten at pageren fulgte; hopper nå dit (`65cca28`)
- Hopp-til-sikting → blokk-bytte scroller kortets topp til toppen av skjermen
- Rest-innstillinger: scrollbar + KAV + forklaringstekst + presets som m:ss
- Tastatur over bunn-sheets: KAV `behavior="height"` → `"padding"` (dødt med edge-to-edge)
- Komma OG punktum som desimalskilletegn (`parseDecimal` i format.ts, alle vekt-inputs)
- Vektforslag fra FØRSTE arbeidssett i forrige økt (ikke siste) + warmup-ekskludering (fikset også latent bug der oppvarmingsrampens sett kunne bli «Sist»)
- Fritt øvelsesbytte: swap-modalen søker hele biblioteket; valg registreres som permanent alternativ (`c177382`)
- «Se full historikk»-lenke fra øvelseskortets historikkpanel → Historikk sets-modus forhåndsfiltrert (?exerciseId=)
- Dagbytte fra hjem: dag-chips i «Neste økt»-kortet + `?day=`-param til loggen
- Rekkefølge i supersett: swap-vert-knapp roterer slots per økt (`applySupersetRotations`, 5 tester) (`9ce62ba`)
- Skip øvelse med grunn: ny `skipped_exercises`-tabell, grunn-dialog, gul markering med Angre på kortet, «Hoppet over»-seksjon i økt-detaljen (`832ccff`)

**Fantes allerede (tester kjørte eldre versjon):**
supersett-redigering i programbyggeren (slot-bytte/A-B/±C), felles runde-CTA i supersett, notat per sett (EditSetModal + rest-bar quick note), auto-avslutt-prompt + «Avslutt økt»-knapp i bunnen, kalender-modalens exit-knapp, «Fortsett økt»-kort på hjem, all-time historikk (sets-modus), chip-crop (v0.14 bleed-fiks — be tester verifisere).

## Verifisering
- tsc 0 feil, 272 tester grønne (11 nye), lint 0 errors (152 warnings, netto −6).
- Alt kun på branch `claude/autobuild-deploy-github-merge-664bda` — IKKE merget til main, IKKE bygget. Marius sier fra når neste Play-versjon skal ut.

## Neste
- Ny testrunde på v0.14.1 + disse endringene når neste build går ut.
- Ved neste release: bump-version, patch notes for alt siden 0.14.1, merge til main, bygg.

## Slippet: v0.15.0-beta (vc17) — ute paa alpha
- Marius ga klarsignal samme dag: bump minor, patch notes (9 punkter nb+en), CHANGELOG, merge til main (CI groenn — guard detekterte bump, skippet pga manglende EXPO_TOKEN som designet), EAS-bygg 17ee8c92 groent paa foerste forsoek, eksplisitt submit, «Hva er nytt» satt. Bekreftet i Play API: track=alpha, versionCodes=[17], completed, notes=en-US.
